apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "resume-tailor.fullname" . }}-config
  labels:
    {{- include "resume-tailor.labels" . | nindent 4 }}
data:
  LOG_LEVEL: {{ .Values.config.logLevel | quote }}
  WORKERS: {{ .Values.config.workers | quote }}
