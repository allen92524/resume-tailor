apiVersion: v1
kind: Service
metadata:
  name: {{ include "resume-tailor.fullname" . }}
  labels:
    {{- include "resume-tailor.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "resume-tailor.selectorLabels" . | nindent 4 }}
