apiVersion: v1
kind: Secret
metadata:
  name: {{ include "resume-tailor.fullname" . }}-secret
  labels:
    {{- include "resume-tailor.labels" . | nindent 4 }}
type: Opaque
data:
  ANTHROPIC_API_KEY: {{ .Values.apiKey | b64enc | quote }}
